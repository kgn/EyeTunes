{
    IBClasses = (
        {
            ACTIONS = {goButtonPressed = id; }; 
            CLASS = DebugController; 
            LANGUAGE = ObjC; 
            OUTLETS = {goButton = NSButton; output = NSTextView; window = NSWindow; }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}